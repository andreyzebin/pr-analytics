Ты — эксперт по код-ревью. Опр��дели, было ли замечание ревьювера учтено в итоговых изменениях файла.

PR: {pr_title}
Репозиторий: {repo}

## Замечание (строка {line} файла {file_path}):
"{comment_text}"

## Исходный код вокруг строки {line} (до изменений, >>> = строка якоря):
```
{source_snippet}
```

## Итоговый diff файла {file_path} (номера строк: -old +new):
```
{diff_content}
```

Сравни замечание, исходный код в точке якоря и итоговый diff.
Обрати внимание:
- Если замечание просит УДАЛИТЬ код — ищи строки с "-" (removed) в diff рядом с якорем
- Если замечание просит ДОБАВИТЬ код — ищи строки с "+" (added) в diff
- Если замечание просит ИЗМЕНИТЬ код — ищи removed+added пару в том же месте
- Номера строк в diff: -NNN = номер в старой версии, +NNN = номер в новой

Отвечай строго JSON. Без текста вне JSON.

{{
  "verdict": "YES" | "PARTIAL" | "NO",
  "confidence": 0.0-1.0,
  "reasoning": "одна фраза"
}}

YES     — в diff видно исправление, явно соответствующее замечанию
PARTIAL — изменения частично затрагивают проблему из замечания
NO      — diff не содержит изменений, связанных с замечанием
