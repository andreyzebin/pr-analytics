Ты — эксперт по анализу код-ревью.

PR: {pr_title}
Репозиторий: {repo}

Комментарий{location}:
"{comment_text}"

Выполни ДВЕ задачи:

1. Классифицируй тип замечания — выбери ОДИН из:
   СТИЛЬ              — отступы, именование, форматирование, порядок импортов
   ПОВЕРХНОСТНАЯ_ЛОГИКА — очевидный баг, опечатка, неправильный тип, не то условие
   ГЛУБОКАЯ_ЛОГИКА    — race condition, deadlock, сложный баг, неочевидные последствия
   АРХИТЕКТУРА        — нарушение SOLID, anti-pattern, неправильный слой, связанность
   ПРОИЗВОДИТЕЛЬНОСТЬ — O(n²) вместо O(n), лишние запросы, неэффективные структуры
   БЕЗОПАСНОСТЬ       — SQL injection, XSS, утечка данных, отсутствие валидации
   ТЕСТЫ              — плохой тест, отсутствие кейса, тест реализации вместо поведения
   БИЗНЕС_ЛОГИКА      — неверное бизнес-правило, неучтённый кейс, неправильный статус
   УСТОЙЧИВОСТЬ       — игнорирование ошибки, потеря контекста, неправильная паника
   ЧИТАЕМОСТЬ         — магические числа, слишком длинная функция, неочевидный код

2. Оцени глубину замечания по шкале 1–3:
   1 — поверхностное: очевидно без знания контекста («переименуй», «добавь пробел»)
   2 — среднее: требует понимания конкретного кода («здесь NPE», «O(n²) из-за вложенного цикла»)
   3 — глубокое: требует понимания архитектуры, домена или неочевидных последствий
       («при двух параллельных вебхуках этот статус затрётся», «нарушает GDPR для EU-юзеров»)

Отвечай строго JSON. Без текста вне JSON.

{{
  "type": "НАЗВАНИЕ_ТИПА",
  "depth": 1|2|3,
  "confidence": 0.0-1.0
}}
